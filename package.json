{
  "name": "spa",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --watch",
    "serve": "live-server --entry-file=index.html --watch=index.html,static,dist"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "webpack": "^5.37.0",
    "webpack-cli": "^4.7.0"
  }
}
